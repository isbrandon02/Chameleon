# Chameleon — AI-Powered Object Replacement
ultralytics>=8.0.0
opencv-contrib-python>=4.8.0
streamlit>=1.28.0
numpy>=1.24.0

# AI mode (SD + ControlNet + IP-Adapter): already satisfied by ultralytics' torch
# Additional: pip install diffusers transformers accelerate safetensors

# FFmpeg must be installed on system: brew install ffmpeg
